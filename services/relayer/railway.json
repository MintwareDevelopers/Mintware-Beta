{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "cargo build --release --bin relayer-server && mkdir -p bin && cp target/release/relayer-server bin/"
  },
  "deploy": {
    "startCommand": "./bin/relayer-server",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
