# Testing

## Test Suites

| Suite | Runner | Count | Command |
|---|---|---|---|
| Unit tests | Vitest | ~220 | `pnpm test` |
| Contract tests (Forge) | Forge | 463 pass / 0 fail / 4 skipped | `pnpm forge:test` |
| Rust — edge-auth | cargo | 86 | `cd services/edge-auth && cargo test` |
| Rust — relayer | cargo | 23 | `cd services/relayer && cargo test` |

> **Forge skips (4):** the mainnet-fork harnesses in `contracts-v4/test/fork/` self-skip when
> `BASE_RPC_URL` is unset (v3→v4 migrator, ULV deploy, YPN payment core). Everything else runs.
> **Rust services** (`services/edge-auth`, `services/relayer`) are `cargo clippy -D warnings`
> clean; run each with `cargo test` from its own dir.

> **⛔ Hardhat suite removed (2026-08-12):** the Hardhat/Mocha `MintwareDistributor` tests + the whole
> Hardhat toolchain were deleted with the shelved campaign stack (see [[campaigns_shelved]] /
> `.claude/rules/smart-contracts.md`). `test:all` is now `pnpm test && pnpm forge:test`. Ignore any
> `hardhat:*` / `.cjs` Mocha references below — historical. Vitest campaign suites (calc, epochProcessor,
> swapHook, merkleBuilder, resolveQuote) were removed too; the count dropped accordingly.

> Counts current on `feat/ypn-vault-convergence` (Vitest ≈220 green; Forge 463/0/4). The old
> "256 / 175" and "147/72/36" figures were stale. Covered now: `lib/attribution/*`,
> `lib/rewards/universal/*`, `lib/rewards/vault/weighted*`, plus the x402 lib + auth
> (`lib/x402/*`, `lib/auth/*`). (Removed: the RWA `holdSnapshot`/`holdLocks` and campaign
> suites no longer exist.)
| All | — | — | `pnpm test:all` |

## Vitest Suites (`pnpm test`)

> **Generated** from `**/*.test.ts` by `pnpm context:sync` — never hand-edit between the AUTO
> markers (see `.claude/CONTEXT-MAP.md`). This replaces the old hand list, which drifted after the
> campaign/RWA suites were deleted.

<!-- AUTO:test-suites -->
<!-- generated by scripts/context-sync.mjs — do not edit by hand -->
38 Vitest suites:
- `app/api/(rewards)/cron/universal-distribution-bridge/route.test.ts`
- `app/api/(rewards)/cron/universal-epoch-close/route.test.ts`
- `app/api/(rewards)/cron/universal-pipeline/route.test.ts`
- `lib/attribution/calibration.test.ts`
- `lib/attribution/legacyShape.test.ts`
- `lib/attribution/providers/chainalysis.test.ts`
- `lib/attribution/providers/etherscan.test.ts`
- `lib/attribution/providers/nansen.test.ts`
- `lib/attribution/providers/referrals.test.ts`
- `lib/attribution/providers/zerion.test.ts`
- `lib/attribution/score.test.ts`
- `lib/attribution/sybil.test.ts`
- `lib/auth/gate.test.ts`
- `lib/auth/rbac.test.ts`
- `lib/auth/requireTeam.test.ts`
- `lib/pools/manifest.test.ts`
- `lib/rewards/eas.test.ts`
- `lib/rewards/referral/utils.test.ts`
- `lib/rewards/universal/distributionBridge.test.ts`
- `lib/rewards/universal/epochAllocator.test.ts`
- `lib/rewards/universal/indexer.test.ts`
- `lib/rewards/universal/settler.test.ts`
- `lib/rewards/universal/store.test.ts`
- `lib/rewards/universal/universalPipeline.test.ts`
- `lib/rewards/vault/weightedDistribution.test.ts`
- `lib/rewards/vault/weightedEpochCloser.test.ts`
- `lib/rewards/vault/weightedEpochOrchestrator.test.ts`
- `lib/web2/routeHandler.test.ts`
- `lib/web3/signedActionMessages.test.ts`
- `lib/web3/vault/treasuryProvisioning.test.ts`
- `lib/x402/edgeHttp.test.ts`
- `lib/x402/facilitator.test.ts`
- `lib/x402/integration.test.ts`
- `lib/x402/pricing.test.ts`
- `lib/x402/protocol.test.ts`
- `lib/x402/require402.test.ts`
- `lib/x402/treasury.test.ts`
- `lib/x402/trustSources.test.ts`
<!-- /AUTO:test-suites -->

## Hardhat Notes

- Config: `hardhat.config.cts` (`.cts` forces CJS with `"type":"module"`)
- Tests: `contracts/test/MintwareDistributor.test.cjs` (`.cjs` for Mocha 10.x)
- Always use: `TS_NODE_PROJECT=tsconfig.hardhat.json` (baked into `hardhat:*` scripts)
- Never use `.ts`/`.cts` for test files — Mocha CJS-in-ESM bridge requires `.cjs`

## Forge Notes

- Binary: `~/.foundry/bin/forge`
- Add to PATH: `export PATH="$HOME/.foundry/bin:$PATH"`
- Tests in `contracts-v4/test/` — currently stubbed (Phase 2 T1 work)
- `contracts-v4/out/` gitignored

## Key Invariants to Test

- Leaf encoding: `abi.encode` (64-byte), NOT `abi.encodePacked` (52-byte)
- Score multipliers applied at point-credit time only (not payout time)
- `pending_rewards` unique constraint: `(campaign_id, tx_hash, reward_type)`
- Oracle EIP-712 includes `deadline` in typed data
- `verifySwapTx` checks `LIFI_ROUTERS` set (not single address)

## New Coverage (this branch)

- **MEV engine invariants — 7/7 green @ 256 runs × 128k depth** (dynamic/surge/quad fee,
  MEV-tax, am-AMM levers; all off/inert by default).
- **x402 lib + auth** — `lib/x402/*` (protocol, pricing, facilitator, treasury, trust-tiering,
  require402) and `lib/auth/*` (gate, rbac, requireTeam, session) have Vitest coverage.
- **v3→v4 migrator fork harness** — `contracts-v4/test/fork/Mintwarev3ToV4MigratorFork.t.sol`
  (self-skips without `BASE_RPC_URL`; part of the 4 Forge skips).
